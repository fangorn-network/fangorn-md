# node_modules is NOT copied — the image runs `pnpm install --prod`.
# Keeping it out drops the build context from ~1.4GB to a few MB.
# dist IS copied — it's built on a dev machine, not in the image.
node_modules
.git
.env
.fangorn
docs
README.md
commit.json
